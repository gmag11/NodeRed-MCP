## 1. SKILL.md

- [ ] 1.1 Create `.github/skills/nodered-flow-builder/SKILL.md` with YAML frontmatter (name, description, when to use)
- [ ] 1.2 Write "Workflow A: Build from scratch" section with numbered steps: create-flow → create-node (with coordinate guidance) → connect-nodes → get-flow-diagram
- [ ] 1.3 Write "Port numbering" callout: output ports are 0-indexed; a switch node with 3 outputs uses ports 0, 1, 2; illustrate with ASCII
- [ ] 1.4 Write "Coordinate grid" section: recommended starting position, horizontal/vertical increment values, note about locked flows
- [ ] 1.5 Write "Workflow B: Import from JSON" section: when to prefer `import-flow`, how to handle conflict strategies
- [ ] 1.6 Write "Workflow C: Edit existing node" section: get-node-detail → update-node → verify
- [ ] 1.7 Write "Workflow D: Delete a flow or node" section with warnings about orphaned connections
- [ ] 1.8 Write "Verification" section: get-flow-diagram for visual check, get-flow-nodes for node inventory
